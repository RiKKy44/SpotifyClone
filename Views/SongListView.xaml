<UserControl x:Class="GLab6.Views.SongListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700">
    <Grid Background="White">
        <DataGrid ItemsSource="{Binding Songs}" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" SelectionUnit="FullRow"
                  CanUserAddRows="False" HeadersVisibility="Column"
                  GridLinesVisibility="None" RowHeight="35"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="Cover" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid Width="24" Height="24" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Rectangle Fill="#555555" RadiusX="2" RadiusY="2"/>
                                <Image Source="{Binding CoverData, Converter={StaticResource BytesToImageConverter}}" Stretch="UniformToFill"/>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="2*"/>
                <DataGridTextColumn Header="Album" Binding="{Binding Album}" Width="2*"/>
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="1*"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration, StringFormat={}{0:m\\:ss}}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>