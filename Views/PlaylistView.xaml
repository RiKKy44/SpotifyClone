<UserControl x:Class="GLab6.Views.PlaylistView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:conv="clr-namespace:GLab6.Converters"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="700">
    <UserControl.Resources>
        <conv:BytesToImageConverter x:Key="BytesToImageConverter"/>
    </UserControl.Resources>

    <Grid Background="White">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid Grid.Column="0" Width="100" Height="100">
                <Rectangle Fill="#EEEEEE" RadiusX="8" RadiusY="8"/>
                <Border CornerRadius="8">
                    <Border.Background>
                        <ImageBrush ImageSource="{Binding CoverData, Converter={StaticResource BytesToImageConverter}}" Stretch="UniformToFill"/>
                    </Border.Background>
                </Border>
            </Grid>

            <StackPanel Grid.Column="1" Margin="20,0,0,0" VerticalAlignment="Center">
                <TextBlock Text="{Binding Name}" FontSize="28" FontWeight="Bold" Foreground="#2A2A35"/>
                <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                    <TextBlock Text="{Binding SongCount}" FontSize="14" Foreground="#666"/>
                    <TextBlock Text=" Songs" FontSize="14" Foreground="#666"/>
                </StackPanel>
                <Button Content="▶ Play" Command="{Binding PlayCommand}" HorizontalAlignment="Left" Padding="20,6" Background="#E91E63" Foreground="White" FontWeight="Bold" BorderBrush="Transparent" Cursor="Hand"/>
            </StackPanel>
        </Grid>

        <DataGrid Grid.Row="1" ItemsSource="{Binding PlaylistSongs}" 
                  AutoGenerateColumns="False" IsReadOnly="True" 
                  SelectionMode="Single" SelectionUnit="FullRow"
                  CanUserAddRows="False" HeadersVisibility="Column"
                  GridLinesVisibility="None" RowHeight="45"
                  AlternatingRowBackground="#F9F9F9">
            <DataGrid.Columns>
                <DataGridTemplateColumn Header="" Width="50">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Grid Width="36" Height="36" HorizontalAlignment="Center" VerticalAlignment="Center">
                                <Rectangle Fill="#EEEEEE" RadiusX="4" RadiusY="4"/>
                                <Border CornerRadius="4">
                                    <Border.Background>
                                        <ImageBrush ImageSource="{Binding CoverData, Converter={StaticResource BytesToImageConverter}}" Stretch="UniformToFill"/>
                                    </Border.Background>
                                </Border>
                            </Grid>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
                <DataGridTextColumn Header="Title" Binding="{Binding Title}" Width="2*"/>
                <DataGridTextColumn Header="Artist" Binding="{Binding Artist}" Width="2*"/>
                <DataGridTextColumn Header="Album" Binding="{Binding Album}" Width="2*"/>
                <DataGridTextColumn Header="Genre" Binding="{Binding Genre}" Width="1*"/>
                <DataGridTextColumn Header="Duration" Binding="{Binding Duration, StringFormat={}{0:m\\:ss}}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>